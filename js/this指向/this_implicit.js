/**
 * 隐式绑定：作为对象的方法调用，this 指向当前执行上下文
 * 当一个函数被调用时，会创建一个执行上下文，它包含函数在哪里被调用（调用栈）、函数调用方式、传入的参数等信息，
 * this就是这个上下文，它会在函数执行过程中被用到
 */
var a = 1;
var obj = {
  a: 2,
  foo: function() {
    console.log(this.a);  
  }
}

obj.foo();  // 2